/** @deprecated import from `@/lib/llm` — kept for compatibility. */
export {
  mockExtraction,
  extractFromImages,
  writeSeasonalityProse,
  writeItineraryProse,
  llmConfigured,
  resolveLlmProvider,
} from "./llm";
